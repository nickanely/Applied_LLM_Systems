Inspection and initial assessment completed.

What I did and why
- I started by inspecting the dataset metadata to understand the schema, data types, and the presence of missing values. This is a foundational step to determine whether imputation or column dropping is needed and to plan subsequent cleaning and feature engineering.

What the metadata reveals
- Shape: 3333 rows, 11 columns.
- Columns: Churn, AccountWeeks, ContractRenewal, DataPlan, DataUsage, CustServCalls, DayMins, DayCalls, MonthlyCharge, OverageFee, RoamMins.
- Data types: 
  - Churn, AccountWeeks, ContractRenewal, DataPlan, CustServCalls, DayCalls are int64.
  - DataUsage, DayMins, MonthlyCharge, OverageFee, RoamMins are float64.
- Missing values: 0 nulls in every column (null_counts all zero, null_percentages all 0.0).

What I learned from the subsequent targeted checks
- DataUsage
  - Mean: 0.8165
  - Median: 0.0
  - Std dev: 1.2727
  - Min: 0.0, Max: 5.4
  - Interpretation: Median at 0.0 with a relatively high standard deviation and a max of 5.4 suggests many users have little to no data usage, indicating a right-skewed distribution with potential sparsity in the feature.
- DayMins
  - Mean: 179.78
  - Median: 179.4
  - Std dev: 54.47
  - Min: 0.0, Max: 350.8
  - Interpretation: Reasonable range with most values around the mean; a few zeros indicate some customers might not make any daytime calls.
- MonthlyCharge
  - Mean: 56.31
  - Median: 53.5
  - Std dev: 16.43
  - Min: 14.0, Max: 111.3
  - Interpretation: Typical telecom pricing spread, with a broad but plausible range.
- Churn (target)
  - Mean: 0.145
  - Median: 0.0
  - Std dev: 0.352
  - Min: 0, Max: 1
  - Interpretation: Class imbalance with roughly 14.5% of customers churned (value 1) and 85.5% not churned (value 0). No missing values.

Decisions taken (and rationale)
- Do not impute missing values: There are zero missing values across all columns. Imputation would be unnecessary and could introduce artificial information.
- Do not drop any columns at this stage: All columns have valid data types and values. There is no evidence from the metadata that a column is redundant or entirely uninformative. Dropping columns without domain-driven justification could remove potentially valuable information.
- Proceed with exploratory statistics to guide feature engineering: I retrieved detailed statistics for a subset of numeric features to assess distributions and potential outliers. This helps inform any future scaling, binning, or feature engineering steps.

Recommended next steps (technical plan)
- Consider feature engineering (optional, based on modeling goals):
  - DataUsage: Explore creating a binary indicator for zero usage vs non-zero, and possibly a ratio feature (DataUsage per DataPlan or per AccountWeeks) if context allows.
  - DayMins, MonthlyCharge, OverageFee, RoamMins: Investigate percentile-based binning or interaction terms (e.g., DayMins * DataUsage) if modeling suggests non-linear effects.
  - Create a churn-aware feature set by validating the class balance when sampling or during model evaluation (e.g., use stratified splits).
- Validate data quality with additional checks not covered by metadata:
  - Check for duplicate rows.
  - Verify that integer-coded columns (Churn, AccountWeeks, ContractRenewal, DataPlan, CustServCalls, DayCalls) align with expected domain codes (e.g., 0/1 for boolean-like fields, reasonable ranges for counts).
  - Confirm there are no impossible values (e.g., negative minutes or charges).
- Modeling readiness notes:
  - Churn is binary-like (0/1) but stored as int64. For many ML pipelines, converting to a boolean or categorical type can be beneficial for certain libraries, though in practice many models handle 0/1 integers fine.
  - All features are numeric; no categorical encodings are needed as part of the current dataset. If you decide to introduce categorical treatment (e.g., ContractRenewal as a category with levels), we can recode accordingly.

Summary
- The dataset is clean with no missing values and 11 numeric columns.
- Preliminary statistics indicate expected, plausible distributions, with notable characteristics: DataUsage is highly skewed with many zeros, Churn is imbalanced toward non-churn, and other features have reasonable ranges.
- Based on the current metadata, there is no immediate need to impute or drop columns. The next steps involve targeted feature engineering and additional quality checks to prepare for modeling.


The dataset metadata indicates a clean, complete dataset with 3333 rows and 11 numeric columns and no missing values. Detailed statistics for key numeric features show sensible ranges, with DataUsage exhibiting a skew toward zero and Churn showing clear class imbalance. No columns appear to require dropping or imputation at this stage. Suggested next actions include optional feature engineering to address skewness and potential outliers, and additional quality checks such as duplicates and domain-valid value ranges to further ensure data suitability for downstream modeling.