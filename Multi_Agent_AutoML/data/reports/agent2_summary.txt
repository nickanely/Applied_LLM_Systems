Target Churn identified as the binary outcome. Categorical features ContractRenewal and DataPlan encoded; two new encoded columns introduced and originals dropped. Five interaction features created from numeric columns. Correlation analysis conducted to guide feature selection; top eight features selected for modeling based on their correlations with Churn. Current dataframe reflects encoded features, interactions, and a reduced feature set suitable for modeling while preserving the read-only target.